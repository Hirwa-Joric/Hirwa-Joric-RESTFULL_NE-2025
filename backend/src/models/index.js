const UserModel = require('./userModel');
const ParkingLotModel = require('./parkingLotModel');
const ParkingRecordModel = require('./parkingRecordModel');

module.exports = {
  UserModel,
  ParkingLotModel,
  ParkingRecordModel
}; 